\documentclass{article}
\usepackage{luacode}
\newcommand{\dataPath}{src/_data}

% lua
\begin{luacode}
require("lualibs.lua")
local f = io.open('\dataPath/resume.json', 'r')
local s = f:read('*a')
f:close()
data =  utilities.json.tolua(s)
\end{luacode}

% tex doc
\begin{document}
some document text

\directlua{tex.print(data['test'])}
\end{document}
