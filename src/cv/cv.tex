\documentclass{article}
\usepackage{luacode}
\newcommand{\dataPath}{dist/files}

% lua
\begin{luacode}
require("lualibs.lua")
local f = io.open('\dataPath/resume.json', 'r')
local s = f:read('*a')
f:close()
resume =  utilities.json.tolua(s)
\end{luacode}

% tex doc
\begin{document}
some text
\directlua{tex.print(resume['contact']['name'])}
\directlua{tex.print(resume['contact']['email'])}
\end{document}
